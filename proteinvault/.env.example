# Copy to .env.local (gitignored) and fill in your real value for local dev.
# In production (Cloudflare Pages), set this in Settings -> Environment
# variables instead — never commit the real value.
VITE_ADMIN_EMAIL=you@example.com
