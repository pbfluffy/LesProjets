// Nutritions Thailand config — environment-level constants.

// Google OAuth 2.0 Client ID for Drive AppData sync (feature #22).
// Created in Google Cloud Console with `drive.appdata` scope.
// Safe to commit — public client ID; auth is gated by the OAuth consent
// screen + the test-users allowlist, not by secrecy of this string.
export const GOOGLE_OAUTH_CLIENT_ID =
  '151312206319-avopaop3mks239rb258be0p6kmh3gsb9.apps.googleusercontent.com'
